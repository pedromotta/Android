<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".activities.MainActivity">

    <Button
        android:id="@+id/constraint_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Constraint"
        android:onClick="startConstraintActivity"/>

    <Button
        android:id="@+id/linear_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Linear"
        android:onClick="startLinearActivity"/>

    <Button
        android:id="@+id/relative_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Relative"
        android:onClick="startRelativeActivity"/>

    <Button
        android:id="@+id/scrolling_views_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Scrolling View"
        android:onClick="startScrollingViewActivity"/>

    <Button
        android:id="@+id/address_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Address"
        android:onClick="startAddressActivity"/>


</LinearLayout>